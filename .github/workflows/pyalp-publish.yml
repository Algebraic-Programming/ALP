name: legacy pyalp-publish (DEPRECATED)

# This workflow was renamed to `publish-to-testpypi.yml` and is now deprecated.
# Keep a disabled stub for auditing/history to avoid accidental runs.
on:
  workflow_dispatch: {}

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated; use publish-to-testpypi.yml instead."
